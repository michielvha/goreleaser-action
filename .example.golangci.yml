# TODO: Look into properly implementing this. Currently using a basic config.
version: "2"

run:
  timeout: 5m

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - gocritic

formatters:
  enable:
    - gofumpt
    - goimports
    - gci
    - gofmt
    # - golines

# Reference: https://golangci-lint.run/usage/configuration/
